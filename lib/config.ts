const config = {
  apiBaseUrl: process.env.NEXT_PUBLIC_API_BASE_URL || 'https://eloco.up.railway.app',
  nextAuthUrl: process.env.NEXTAUTH_URL || 'https://eloco.up.railway.app',
};

export default config;
